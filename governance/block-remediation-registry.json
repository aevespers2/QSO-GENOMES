{
  "registry_version": 3,
  "authority": {
    "canonical_id": "jacob_thomas_redmond",
    "display_name": "Jacob Thomas Redmond"
  },
  "default_disposition": "block_pending_human_review",
  "rules": {
    "schema_validation": {
      "description": "Validate the exact genome artifact set and every schema-bound artifact.",
      "commands": [
        ["python", "scripts/validate_schema_set.py"]
      ]
    },
    "immutable_ethics": {
      "description": "Validate immutable ethics migration and local-ethics conflict boundaries.",
      "commands": [
        ["python", "scripts/validate_immutable_ethics_migration.py"]
      ]
    },
    "active_identity_migration": {
      "description": "Verify retired review identifiers cannot resolve through active governance surfaces.",
      "commands": [
        ["python", "-m", "unittest", "discover", "-s", "tests", "-p", "test_disengaged_identity_migration.py", "-v"]
      ]
    },
    "manifest_identity": {
      "description": "Regenerate and verify identity-bearing compatibility-manifest semantics.",
      "commands": [
        ["python", "scripts/generate_contract_manifest.py", "--check"]
      ]
    },
    "full_conformance": {
      "description": "Run all bounded, repository-local conformance checks.",
      "commands": [
        ["python", "scripts/validate_schema_set.py"],
        ["python", "scripts/validate_immutable_ethics_migration.py"],
        ["python", "scripts/generate_contract_manifest.py", "--check"],
        ["python", "-m", "unittest", "discover", "-s", "tests", "-v"]
      ]
    }
  }
}
